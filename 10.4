#include <iostream>
using namespace std;
#include <cmath>
int main()
{
    
int n, i, d;
   double a;
    d = 0;
    cin >> n >> a;
    if (n > 0)
    {
        for (i = 0; i <= n; i++)
       {
      d += pow (a, i);
        }
        cout << d  << ends;
    }
    else{
        cout << "error" << ends;
    }
    
 
}
