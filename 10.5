#include <iostream>
using namespace std;
#include <cmath>
int main()

{

int n, i, d, s;
double a;
    d = 0;
    s = 1;
    cin >> n >> a;
    if (n > 0){
        for (i = 1; i <= n; i++)
        { 
            d += s;
            s = -1 * pow (a, i);
        }
        cout << d  << ends;
    }
    else
    {
        cout << "error" << ends;
    }
    
  
}
